{
  plugins = {
    image = {
      autoLoad = true;
      enable = true;
      settings = {
        backend = "ueberzug";
        integrations = {
          markdown.enabled = true;
        };
      };
    };
  };
}
