{
    plugins = {
        notify = {
            enable = true;
            fps = 60;
        };
        noice = {
            enable = true;
            presets = {
                command_palette = true;
            };
        };
    };
}
